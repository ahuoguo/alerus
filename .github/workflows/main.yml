name: Verify with Verus

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Rust 1.95.0
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0

      - name: Download Verus
        run: |
          wget https://github.com/verus-lang/verus/releases/download/release%2F0.2026.06.14.4ea7d0f/verus-0.2026.06.14.4ea7d0f-x86-linux.zip
          unzip verus-0.2026.06.14.4ea7d0f-x86-linux.zip

      - name: Add Verus to PATH
        run: echo "$(pwd)/verus-x86-linux/" >> $GITHUB_PATH

      - name: cargo verus verify -p alerus
        working-directory: .
        run: cargo verus verify -p alerus
